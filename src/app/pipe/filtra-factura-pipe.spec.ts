import { FiltraFacturaPipe } from './filtra-factura-pipe';

describe('FiltraFacturaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraFacturaPipe();
    expect(pipe).toBeTruthy();
  });
});
