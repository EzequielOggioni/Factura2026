import { FitraUserPipe } from './fitra-user-pipe';

describe('FitraUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FitraUserPipe();
    expect(pipe).toBeTruthy();
  });
});
